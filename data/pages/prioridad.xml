<form title="Prioridad" subtitle="Seleccionar Prioridad">
  <sql caption="Prioridad">
SELECT [nid_prioridad]
      ,[no_nombre]
  FROM [MST_PRIORIDAD]
    <column name="no_nombre" caption="Nombre" />
    <column name="nid_prioridad" caption="Identificador" readonly="1" />    
    <update>
    update [MST_PRIORIDAD] SET %%FIELDVALUE%% WHERE %%KEY%%
    </update>
    <insert>
    insert into [MST_PRIORIDAD] (%%FIELDS%%) values(%%VALUES%%)
    </insert>
    <delete>
    delete from [MST_PRIORIDAD] WHERE %%KEY%%
    </delete>
  </sql>
</form>